import { AppConfigModule } from './app-config.module';
import { AppConfigService } from './app-config.service';
import appConfig from './app-config';

export { AppConfigModule, AppConfigService, appConfig };
